{
  "name": "@repo/email",
  "version": "0.0.0",
  "description": "This package holds the email logic.",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc",
    "clean": "rm -rf dist",
    "remove:pkg": "rm -rf node_modules",
    "rebuild": "pnpm run clean && pnpm run remove:pkg && pnpm install && pnpm run build"
  },
  "keywords": [
    "email"
  ],
  "dependencies": {
    "dotenv": "^16.4.7",
    "nodemailer": "^6.10.0",
    "@repo/email-templates": "workspace:*"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/nodemailer": "^6.4.17",
    "@types/dotenv": "^8.2.3"
  },
  "author": "Mayank Tiwari <devmayanktiwari@gmail.com>",
  "license": "ISC"
}
